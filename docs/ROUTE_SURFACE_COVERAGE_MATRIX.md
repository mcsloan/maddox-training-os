# Route Surface Coverage Matrix

## Purpose

This compact matrix routes future QA coverage by surface. It is not a complete route inventory and is not a test suite.

## Anti-Bloat Warning

One row per surface or surface group. Do not add one row per date. Do not duplicate full contract text. If route/component ownership is unknown, use `Status: Needs route/component inspection`.

## Surface Route Map

| Surface group | Actual route/page file(s) | Primary component file(s) | Shared projection/model dependency | Relevant contract IDs | Mutation risk | Automation readiness | Notes / unknowns |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Today | `app/today/page.tsx`, redirecting to the current-date Day route. | Canonical Day route components after redirect. | Canonical Day route and date resolution in `America/Toronto`. | ABC-001, ABC-002, ABC-011, ABC-012 | Medium after redirect if logging actions are followed. | Route ownership recorded; use Day coverage for surface assertions. | Today must equal canonical Day. |
| Day | `app/day/[date]/page.tsx`. | `components/DayExecutionSequence.tsx`, `components/DayEvidenceStatus.tsx`, `components/LoadChips.tsx`. | v8.4 daily/session imports, `projectDayPresentationContext()`, `projectPlannedDayActivities()`, `buildDayPresentation()`, day evidence projection inputs. | ABC-001, ABC-002, ABC-003, ABC-004, ABC-005, ABC-007, ABC-011 | Medium if start/log buttons are clicked. | Partially ready through existing Day/Session proof route; broader assertions need fixtures. | Keep one row for all dates; dates are fixtures only. |
| Session active/edit | `app/session/[id]/page.tsx`, live/resume/reopen modes. | `components/DrillCard.tsx`, `components/ReadinessCheck.tsx`, `components/ReflectionForm.tsx`, `components/SessionKPIForm.tsx`, `components/SessionProgress.tsx`. | v8.4 session imports, `projectDayPresentationContext()`, `projectPlannedDayActivities()`, `activityToDrill()`, local/cloud session repositories. | ABC-002, ABC-003, ABC-004, ABC-010 | High if save/finish/reopen paths are clicked. | Read-only route assertions partially ready; mutation-safe active/edit coverage needs explicit guardrails. | Active/edit Session uses shared day context through `projectDayPresentationContext()` / `dayContext?.heroTitle`. |
| Completed-session/read-only | `app/session/[id]/page.tsx?mode=view`; Previous Attempt gate and latest-session actions are in `app/session/[id]/page.tsx` mode `choice`. | `components/SessionSummary.tsx`; choice-mode controls live in the Session page. | Saved session records plus session/workout display sources; completed `SessionSummary` now receives canonical display title from `projectDayPresentationContext()` when available and falls back to `workout.dayFocus`. | ABC-003, ABC-004, ABC-008, ABC-010 | Low for view-only assertions; high if Reopen/Edit, Start Fresh, Save, or Finish paths are clicked. | DEF-028 component proof exists; completed-session/read-only route-state fixture is still needed before concrete Playwright expansion. Existing `e2e/activity-presentation-proof.spec.ts` did not exercise this branch. | DEF-028 fixed at `9fd4c73`: View Latest Completed Session sets `/session/${workoutId}?sessionId=...&mode=view`; Reopen / Edit Latest Completed Session sets live mode and uses the active Session surface. Saved session records remain immutable. |
| Calendar | `app/calendar/page.tsx`. | `components/LoadChips.tsx`. | v8.4 calendar/daily/session imports, `buildDayEvidenceProjection()`, `buildCalendarDayProjection()`, `buildDayPresentation()`. | ABC-001, ABC-002, ABC-005, ABC-011 | Low if read-only; medium if Sport Load logging links are followed. | Route ownership recorded; needs fixture-backed status assertions. | Calendar is a date navigation/status projection, not a date-by-date test registry. |
| History | `app/history/page.tsx`. | `components/DataStatus.tsx`. | `loadTrainingHistory()`, KPI/Sport Load repositories, `buildDayEvidenceProjection()`, `buildHistoryDayProjection()`, local session repository. | ABC-001, ABC-006, ABC-007, ABC-009, ABC-010 | Medium because delete, start-fresh, reopen, and resume controls are present. | Needs guardrails and evidence fixtures before browser expansion. | History should remain Week > Day > Evidence; dangerous actions require separate approval. |
| Dashboard / Parent view | `app/dashboard/page.tsx`. | `components/DataStatus.tsx`, `components/ParentDashboardCard.tsx`. | `buildDayEvidenceProjection()`, `buildWeekDashboardProjection()`, training metrics, training plan/week summaries, KPI/Sport Load/session repositories. | ABC-001, ABC-002, ABC-005, ABC-007, ABC-009 | Low/medium depending on links followed; dashboard itself is summary-oriented. | Route ownership recorded; needs fixture-backed parent/athlete consistency assertions. | Parent/operator detail may differ from athlete view but must not contradict facts. |
| KPIs | `app/kpis/page.tsx`. | `components/KPIEntryForm.tsx`. | KPI repository, training plan KPI days, KPI metrics helpers. | ABC-002, ABC-007, ABC-009, ABC-010 | High if add/edit/delete controls are used. | Needs fixture and mutation-safe route plan. | Missing KPI data must not become deferred data. |
| Plan | `app/plan/page.tsx`. | `components/LoadChips.tsx`, `components/WeeklyLoadChart.tsx`. | v8.4 Gantt/phase imports, training plan/week summaries, Sport Load week projections. | ABC-001, ABC-005, ABC-011 | Low if read-only. | Route ownership recorded; read-only coverage can be contract-driven. | Do not polish or rework Gantt unless explicitly asked. |
| Exports / developer tools | `app/exports/page.tsx`. | `components/ExportButtons.tsx`, `components/ClearLocalDataButton.tsx`. | Export helpers and training-data next/today workout lookup. | ABC-001, ABC-006, ABC-007, ABC-009, ABC-010 | High if local clear/reset or future export-generation actions are used. | Manual/source inspection first; dangerous-action guardrails required. | Export generation is currently described as a placeholder; reset browser data is a developer tool. |
| Sport Load logging | `app/external-load/[id]/page.tsx`. | Status: Needs route/component inspection. | v8.4 Sport Load imports and Sport Load evidence repository. | ABC-002, ABC-005, ABC-007, ABC-010 | High if logging is saved. | Needs route/component inspection before test generation. | Sport Load logging must not complete Training Work. |
| Training Work logging | `app/training-work/[date]/page.tsx`. | Status: Needs route/component inspection. | Training Work evidence repository and day/date context. | ABC-002, ABC-003, ABC-004, ABC-010 | High if logging is saved. | Needs route/component inspection before test generation. | Training Work logging must remain separate from Sport Load logging. |
| Admin/debug surfaces | `app/debug/session/[id]/page.tsx`, `app/debug/session-minimal/[id]/page.tsx`, `app/debug/local-data/page.tsx`, `app/debug/plain-js/page.tsx`, `app/debug/react-hydration/page.tsx`. | Status: Needs route/component inspection. | Raw/source/debug data and storage diagnostics. | ABC-001, ABC-010 | Medium/high depending on controls. | Manual/source inspection first. | Admin/debug surfaces are not athlete-facing contract proof by themselves. |
