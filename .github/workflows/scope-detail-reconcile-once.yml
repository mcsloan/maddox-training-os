name: Scope Detail Reconcile Once

on:
  push:
    branches:
      - docs/scope-reconcile-2026-08-21

permissions:
  contents: write

jobs:
  reconcile:
    if: ${{ !contains(github.event.head_commit.message, '[scope-detail-reconciled]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: docs/scope-reconcile-2026-08-21
          fetch-depth: 0

      - name: Reconcile detailed scope records
        run: node scripts/reconcile-scope-detail-2026-08-21.mjs

      - name: Verify diff
        run: |
          git diff --check
          git diff -- docs/SCOPE.md

      - name: Remove one-shot machinery
        run: |
          rm scripts/reconcile-scope-detail-2026-08-21.mjs
          rm .github/workflows/scope-detail-reconcile-once.yml

      - name: Commit reconciled details
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/SCOPE.md scripts/reconcile-scope-detail-2026-08-21.mjs .github/workflows/scope-detail-reconcile-once.yml
          git commit -m "docs(scope): reconcile detailed status evidence [scope-detail-reconciled]"
          git push
